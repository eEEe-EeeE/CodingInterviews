import StringMatch.KMPMatcher;
import StringMatch.Result;

import java.nio.charset.Charset;
import java.util.*;
import java.util.regex.Matcher;
import java.util.regex.Pattern;


public class Main {

    public static void main(String[] args) {
        System.out.println(-11 % -3);
    }

    static int divide(int dividend, int divisor) {
        if (divisor == 0)
            throw new ArithmeticException("divisor is zero.");
        if (dividend == 0)
            return 0;
        boolean signIdentical;
        boolean dividendPositive;
        signIdentical = (dividend & Integer.MIN_VALUE) == (divisor & Integer.MIN_VALUE);
        dividendPositive = (dividend & Integer.MIN_VALUE) == 0;

        return 0;
    }

}
